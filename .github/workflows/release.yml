name: Release
on:
  push:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/build.yml

  release:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          command: manifest
